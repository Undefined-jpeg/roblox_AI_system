# Real secrets/config -- keep Config.example.lua checked in, not this.
src/server/Config.lua

# Rojo build output, if anyone runs `rojo build` locally.
*.rbxlx
*.rbxl

# Proxy server (Node) -- also has its own .gitignore, this is a backstop.
proxy/node_modules/
proxy/.env
